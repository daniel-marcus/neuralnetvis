pre-commit:
  commands:
    format:
      glob: "*.{ts,tsx,js,jsx,json,css,scss}"
      run: pnpm fmt && git add {staged_files}
    typecheck:
      run: pnpm typecheck
    lint:
      run: pnpm lint
    test:
      run: pnpm test
